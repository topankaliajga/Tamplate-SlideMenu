import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'project ke 2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
